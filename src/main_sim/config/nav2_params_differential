amcl:  # if used later instead of SLAM
  ros__parameters:
    use_sim_time: true

controller_server:
  ros__parameters:
    use_sim_time: true
    controller_frequency: 10.0
    use_holonomic: false
    controller_plugins: ["FollowPath"]

    FollowPath:
      plugin: "dwb_core::DWBLocalPlanner"
      debug_trajectory_details: false
      min_vel_x: -0.26
      max_vel_x: 0.26
      max_vel_theta: 1.0
      min_vel_theta: -1.0
      acc_lim_x: 2.5
      acc_lim_theta: 3.2

      # No y velocity here — differential drive
      critics: ["ObstacleFootprintCritic", "GoalAlignCritic", "PathAlignCritic", "PathDistCritic", "GoalDistCritic"]
      ObstacleFootprintCritic:
        scale: 0.02
      GoalAlignCritic:
        scale: 24.0
      PathAlignCritic:
        scale: 32.0
      PathDistCritic:
        scale: 32.0
      GoalDistCritic:
        scale: 24.0

planner_server:
  ros__parameters:
    expected_planner_frequency: 1.0
    use_sim_time: true
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_navfn_planner/NavfnPlanner"

behavior_server:
  ros__parameters:
    use_sim_time: true
    progress_check_plugins: ["progress_checker"]
    goal_checker_plugins: ["goal_checker"]
    progress_checker:
      plugin: "nav2_behaviors/ProgressChecker"
    goal_checker:
      plugin: "nav2_behaviors/GoalChecker"

bt_navigator:
  ros__parameters:
    use_sim_time: true
    default_bt_xml_filename: "navigate_w_replanning_and_recovery.xml"
    behavior_tree_nav_plugins: ["navigate_to_pose"]

local_costmap:
  ros__parameters:
    use_sim_time: true
    robot_base_frame: "base_link"
    update_frequency: 5.0
    publish_frequency: 2.0
    global_frame: "odom"
    rolling_window: true
    width: 3.0
    height: 3.0
    resolution: 0.05
    footprint: "[[-0.2, -0.25], [-0.2, 0.25], [0.2, 0.25], [0.2, -0.25]]"
    plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
    static_layer:
      plugin: "nav2_costmap_2d::StaticLayer"
    obstacle_layer:
      plugin: "nav2_costmap_2d::ObstacleLayer"
    inflation_layer:
      plugin: "nav2_costmap_2d::InflationLayer"

global_costmap:
  ros__parameters:
    use_sim_time: true
    robot_base_frame: "base_link"
    update_frequency: 5.0
    publish_frequency: 2.0
    global_frame: "map"
    footprint: "[[-0.2, -0.25], [-0.2, 0.25], [0.2, 0.25], [0.2, -0.25]]"
    plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
    static_layer:
      plugin: "nav2_costmap_2d::StaticLayer"
    obstacle_layer:
      plugin: "nav2_costmap_2d::ObstacleLayer"
    inflation_layer:
      plugin: "nav2_costmap_2d::InflationLayer"

waypoint_follower:
  ros__parameters:
    use_sim_time: true
    stop_on_failure: false
    loop_rate: 20
